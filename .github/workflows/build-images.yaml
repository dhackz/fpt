name: build-containers
on:
  - pull_request

jobs:
  build-containers:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - frontend
          - backend
    steps:
      - name: Build docker image
        run: docker build -t fpt/fpt-${{ matrix.service }} ${{ matrix.service }}
