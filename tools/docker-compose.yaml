version: '3'

services:
  reverse-proxy:
    image: nginx
    network_mode: host
    volumes:
      - "./conf:/etc/nginx/conf.d"
  redis-primary:
    image: redis:alpine
    ports:
      - "6379:6379"
